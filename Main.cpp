#include "Organizer.h"

int main(){

Organizer Org;

/*
Patient a;
Patient b;
Patient c;
a.setRequestTime(1);
b.setRequestTime(1);
c.setRequestTime(1);
a.setSev(1);
b.setSev(2);
c.setSev(3);
priQueue<Patient> Q;
Q.enqueue(a, a.getSeverity());
Q.enqueue(b, b.getSeverity());
Q.enqueue(c, c.getSeverity());
*/
}